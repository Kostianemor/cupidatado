class MyClass {
    constructor(parent) {
        // Constructor logic goes here
        this.parent = parent; // Example of assigning a property based on constructor parameter
    }
}

// Creating an instance of MyClass
let obj = new MyClass(someParent); // 'someParent' is passed as the 'parent' parameter to the constructor
